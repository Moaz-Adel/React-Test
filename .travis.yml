sudo: required
language: generic

services:
 - docker

before _install:
  -docker build -t Moaz-Adel/React-Test -f Dockerfile.dev .

scripts:

  -docker run -e CI=true Moaz-Adel/React-Test npm run test 